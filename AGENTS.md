# Project overview

这是“旅行照片 → 参考图艺术化 → 单文件离线翻页画册”的跨智能体 Skill。当前阶段聚焦来源与宿主能力核查、风格小样门槛和翻页原型。

关键目录：

- `docs/`：设计、来源许可、能力与阶段证据。
- `references/styles/`：按需加载的三种艺术风格规范。
- `scripts/`：画册构建与项目检查。
- `tests/`：真实浏览器 smoke test。
- `vendor/`：允许分发的第三方资源及许可。
- `private-acceptance/`：用户私有照片、清单、样片和成品；永不提交。

# Working rules

- 原图只读，不递归子目录，不静默跳过 HEIC 或损坏文件。
- 用户验收一未确认前，不得批量生图。
- 人物辨识度优先于艺术效果；不得新增人物、交换身份或改变核心事实。
- 生图必须使用对应原图作为参考，不把具体工具名写成通用宿主接口。
- 画册构建脚本只消费已确认成图，不调用生图、不推断私人故事。
- 不复制无明确许可的 `photo-album-skill` 代码。
- 不提交照片、运行记录、密钥、本机绝对路径、依赖目录或构建产物。
- 不增加无必要的生产依赖，不做无关重构。

# Commands

- 安装：`npm install`
- 构建原型：`npm run build:prototype`
- 测试：`npm test`
- 静态检查：`npm run check`
- Lint / 类型检查 / 格式化：当前未配置。

# Validation requirements

修改后运行与范围匹配的最小验证，并报告实际命令与结果。涉及画册时至少运行 `npm test`，验证 `file://` 打开、无网络请求、点击/键盘翻页、首尾边界与移动端单页。无法验证 Safari 或真机时必须明确标注。

# Delivery boundaries

不自动 push、发布、修改远程资源或用户级/系统级配置。外部写入、删除、批量生图和高风险操作需要明确确认。
