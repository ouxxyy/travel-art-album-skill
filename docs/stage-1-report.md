# 阶段一交付与运行证据

日期：2026-09-09。

## 已交付

- 固定三项来源版本并核查许可；只分发两项 MIT 来源，未复制无明确许可的 `photo-album-skill`。
- 核查当前宿主文件、脚本、图片查看、参考图编辑、浏览器和 HEIC 基础能力，并明确未验证项。
- 派生三份按需加载的风格规范，移除图内文字，强化人物辨识和横图重构要求。
- 构建“封面 + 两页 + 封底”单文件离线原型，全部视觉素材均为明确标注的占位图。
- 为快速连续翻页加入输入队列，避免动画期间丢失按钮或键盘操作；首页和末页均有外层边界保护。

## 自动化证据

`npm test`：

- 从 `file://` 打开生成的 `dist/prototype.html`。
- 捕获 HTTP(S) 请求并断言为 0。
- 桌面断言 landscape，并验证按钮与键盘快速正向翻页、反向翻页、首页/末页边界。
- 390×844 移动视口断言 portrait，并用真实 TouchEvent 左滑翻页、按钮返回首页。

`npm run check`：

- 检查 13 个必需项目文件。
- 检查生成 HTML 内嵌 StPageFlip 许可且没有外部运行时资源。
- 检查所有拟纳入仓库的文件，阻止常见密钥/数据库扩展名和大于 5 MiB 的文件。

## 独立复核

独立复核首次不放行，指出：Git 基线缺失、反向/移动交互覆盖不足、项目检查过弱。以上测试与检查已按意见补齐；基线提交在最终 fresh 验证和 staged 审查后创建。

## 未完成与下一门禁

- 没有用户照片，九张样片尚未生成；占位原型不代表生图效果。
- Safari 与移动真机没有运行证据。
- 必须由用户提供真实照片并确认风格、人物辨识度与翻页体验，保存用户验收一结果后才能开始批量生图和后续工程实现。
