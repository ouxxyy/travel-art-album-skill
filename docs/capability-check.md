# 当前宿主能力检查

检查日期：2026-09-09；宿主：当前 Codex / Multica 本地运行环境。

| 必需能力 | 结果 | 真实证据/边界 |
| --- | --- | --- |
| 读取与写入文件 | 通过 | 可在指定项目目录读写，原图策略仍规定只读 |
| 执行脚本 | 通过 | Node `v22.22.3`、npm `12.0.2`、Python `3.12.7` |
| 查看图片 | 通过 | 当前宿主暴露本地图片查看能力；尚无用户照片可实测 |
| 参考图编辑 | 通过（接口存在） | 当前宿主图像能力明确支持携带本地参考图进行编辑；尚未用用户照片做质量验收 |
| 真实浏览器检查 | 部分通过 | 本机 Google Chrome/Chromium 与 Playwright 可用；Safari 存在但无自动化驱动，Safari 实测待人工完成 |
| HEIC 解码/方向 | 部分通过 | macOS `sips` 可用；尚未用真实 HEIC 样本验证方向与失败报告 |
| 可靠计费信息 | 不可用 | 不输出费用估算，只在生成前报告预计图片调用次数 |

## 当前阻塞项

1. 未提供真实旅行照片，不能生成九张小样，也不能验证人物辨识度。
2. 人物辨识度只能由用户最终确认，智能体检查不可替代。
3. Safari 与移动真机尚未完成实测，不得宣称已验证支持。
